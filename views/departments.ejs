<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Departments</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f7f7f7;
            margin: 0;
            padding: 40px;
        }

        .container {
            max-width: 1100px;
            margin: auto;
        }

        h2 {
            font-size: 28px;
            margin-bottom: 20px;
        }

        .filters {
            display: flex;
            gap: 20px;
            margin-bottom: 25px;
        }

        input, select {
            padding: 12px 15px;
            width: 100%;
            border: 1px solid #ddd;
            border-radius: 6px;
            font-size: 16px;
            outline: none;
        }

        .table {
            width: 100%;
            border-collapse: collapse;
            background: white;
            border-radius: 10px;
            overflow: hidden;
        }

        th {
            background: #f0f0f0;
            padding: 15px;
            text-align: left;
            font-size: 15px;
            color: #444;
        }

        td {
            padding: 15px;
            border-bottom: 1px solid #eee;
            font-size: 16px;
        }

        tr:last-child td {
            border-bottom: none;
        }

        .edit-btn {
            background: #537785;
            color: white;
            border: none;
            padding: 7px 15px;
            border-radius: 5px;
            cursor: pointer;
        }

        .delete-btn {
            color: #d9534f;
            cursor: pointer;
            background: none;
            border: none;
            margin-left: 10px;
        }
    </style>
</head>

<body>
    <div class="container">
        <h2>All Departments</h2>
        <form action="/departments" method="GET" class="filters">
            <input type="text" 
                    name="search" 
                    placeholder="Serach by Department Name"
                    value = <%=search%>
                    >
            <select name="type">
                    <option value="all" <%= type==="all" ? "selected" : ''%>  >All</option>
                    <option value="UG" <%= type==="UG" ? "selected" : '' %> >UG</option>
                    <option value="PG" <%= type==="PG" ? "selected" : '' %> >PG</option>
                    <option value="Research" <%= type==="Research" ? "selected" : '' %> >Research</option>
            </select>
            <button type="submit">Apply</button>
        </form>

        <table class="table">
        <thead>
            <tr>
                <td style="font-weight: bold;">Department Name</td>
                <td style="font-weight: bold;">Department Type</td>
                <td style="font-weight: bold;">User Count</td>
            </tr>
        </thead>
           <% data.forEach((ele)=>{ %>
                <tr>
                    <td><%=ele.name%></td>
                    <td><%=ele.type%></td>
                    <td><%=ele.userCount%></td>
                    <td><button>Edit</button></td>
                    <td>
                        <form action="/delete_deapartment/<%=ele._id%>"  method="GET" onsubmit="deletePopUp('<%=ele._id%>')">
                            <button >Delete</button>
                        </form>
                    </td>
                </tr>
          <% }) %>
        </table>
    </div>
</body>

<script>
    function deletePopUp(id){
     if(confirm(`Are you Sure you want to delete`)){
     }
     else{
        window.location.href = "/departments"
     }
    }
</script>

</html>
